
@pytest.fixture(scope="class")
def trustyai_operator_configmap():
    # check if exists and yield trustyai operator configmap from py_config["applications_namespace"] (opendatahub/redhat-ods-applications)

@pytest.fixture(scope="class")
def trustyai_operator_pod():
    # check if exists and yield trustyai operator pod from py_config["applications_namespace"] (opendatahub/redhat-ods-applications)
